import React from "react";

// components
import { Button } from "components";

export default () => {
  return (
    <Button type="submit" name="Sign up" className="sign-up" value="Sign up" />
  );
};
